staticSites:
  - name: walking-alerts-frontend
    env: node
    branch: main
    buildCommand: npm install && npm run build
    publishPath: dist
    envVars:
      - key: VITE_API_URL
        value: "https://situa.onrender.com"
